# Docker Compose settings for docs/localhost/compose.yaml.
# Copy this file to docs/localhost/.env before running docker compose.

POSTGRES_DB=latch_works
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_PORT=5432

RUSTFS_API_PORT=9000
RUSTFS_CONSOLE_PORT=9001
RUSTFS_ACCESS_KEY=rustfsadmin
RUSTFS_SECRET_KEY=rustfsadmin
RUSTFS_REGION=auto

S3_BUCKET=latch-works-media
