# Frame View

Frame View is the existing Electron desktop viewer currently maintained at:

```text
C:\Users\Trayd\dev\frame-view
```

Import this working tree here during the consolidation step. Keep it running before extracting shared packages into `packages/media-domain` and `packages/media-ui`.
