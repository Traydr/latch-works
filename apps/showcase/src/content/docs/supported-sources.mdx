---
title: Supported sources
description: Exact sites and content types Gather Box can collect.
section: Reference
sectionOrder: 6
order: 2
navTitle: Supported sources
---

# Supported sources

Gather Box collects only from the sites below. Anything else shows as unsupported in the side panel.

## Site appendix

| Site | Content type | Output |
| --- | --- | --- |
| X | Images and videos | Media under the post author's username |
| Reddit | Images, galleries, GIFs, and video (including RedGIFs) | Direct file, or a post folder for multi-media |
| pixiv | Original artwork (including multi-page) | Images under `username-userId` |
| Archive of Our Own | Works | Single site-generated PDF in the chosen root |
| FanFiction.Net | Multi-chapter stories | Single locally merged PDF in the chosen root |

## Media vs story behavior

**Media collectors** download original files (images and, on some sites, video). Filenames usually come from the site or CDN; Gather Box does not renumber everything to `001`, `002`, and so on. Reddit galleries are the exception: they use `01_<filename>` prefixes. With AVIF conversion on, still images save as `.avif` and GIFs as `.mp4`.

**Story collectors** produce one PDF per work:

- **AO3** downloads the site’s generated PDF.
- **FanFiction.Net** fetches chapters and merges them locally into one PDF.

Story downloads may need you to be signed in. Gather Box can include session cookies. See [Collect stories](/docs/gather-box/collect-stories#credentials-and-login).

## Folder patterns

Paths are relative to the destination folder you pick in the side panel. Spaces become underscores.

| Source | Typical shape |
| --- | --- |
| X | `<root>/<username>/` (first letter lowercased) |
| Reddit | `<root>/` for single media; `<root>/<post title>_<id>/` for galleries |
| pixiv | `<root>/<username>-<user id>/` |
| AO3 / FanFiction.Net | `<root>/Author-Story.pdf` (no nested site folder) |

Details and edge cases are covered in [Gather Box folders](/docs/gather-box/folders).

## When a page is unsupported

The download action stays disabled and the side panel explains that the active tab is not a supported URL pattern. Double-check you are on a post, artwork, or work page, not a profile index or search results page.

## Related docs

- [Gather Box overview](/docs/gather-box)
- [Collect media](/docs/gather-box/collect-galleries)
- [Collect stories](/docs/gather-box/collect-stories)
