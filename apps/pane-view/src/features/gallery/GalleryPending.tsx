import { GalleryGridSkeleton } from "./GalleryGridSkeleton";

/** @deprecated Use GalleryGridSkeleton inside the gallery layout shell. */
export function GalleryPending() {
  return <GalleryGridSkeleton />;
}
