/** Keys the grid's saved scroll offset; the router scrolls it to the top on a new folder. */
export const GALLERY_GRID_SCROLL_ID = "gallery-grid";

/** Finds the FloatingToolbar, so the grid can keep a focused card clear of it. */
export const FLOATING_TOOLBAR_SELECTOR = "[data-floating-toolbar]";
