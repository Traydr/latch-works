# Implementation Plans

Generated by the improve skill on 2026-06-17 for Pane View gallery performance. Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Compute Gallery Derived State Once | P1 | S | - | DONE |
| 002 | Resolve Gallery Delivery Batches Without Per-Item Re-Reads | P1 | M | - | DONE |
| 003 | Add Indexes For Gallery Scope And Search Queries | P1 | M | - | DONE |
| 004 | Move Gallery Sorting Filtering And Pagination To Server Queries | P1 | L | 001, 003 | DONE |
| 005 | Trim Comic Mode Folder Child Lookup Work | P2 | S/M | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 004 depends on 001 because the gallery should have one clear derived-state path before the larger
  server-owned listing refactor.
- 004 depends on 003 because server-side sort/filter/pagination will put more pressure on the
  recursive and search query shapes.
- 005 is independent if run before 004. If 004 fully replaces comic-mode listing, re-evaluate 005 and
  mark it REJECTED as superseded or DONE by replacement.

## Verification baseline

Use these repo commands unless a plan gives a narrower focused command:

| Purpose | Command | Expected on success |
| --- | --- | --- |
| Pane View typecheck | `pnpm --filter @latch-works/pane-view typecheck` | exit 0 |
| Pane View tests | `pnpm --filter @latch-works/pane-view test` | exit 0 |
| Pane View check | `pnpm --filter @latch-works/pane-view check` | exit 0 |
| Media domain tests | `pnpm --filter @latch-works/media-domain test` | exit 0 |

Repository caveats from AGENTS.md:

- Full workspace `pnpm check` has known Linux caveats around `apps/frame-view`.
- Pane View E2E with real media requires PostgreSQL and S3-compatible storage; do not start services
  unless the operator asks.
- `pnpm lint` may report pre-existing Biome format/import issues unrelated to a plan.

## Findings considered and rejected

- Image derivative prewarm looked suspicious because one doc sentence said image/gif/video derivatives
  are prewarmed, but current code and tests make `supportsDerivative` video-only. No plan was written
  for image prewarm waste.

## Audit scope

Audited with focus on Pane View gallery performance. Not audited: live browser performance traces,
live Postgres `EXPLAIN`, S3/CDN runtime behavior, full media-optimizer throughput, and non-gallery
Pane View surfaces.
